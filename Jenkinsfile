pipeline {
    agent any

    stages{
       
        stage("build"){
            steps{
                echo "hello build"
            }
        }
         stage("build"){
            steps{
                echo "hello test"
            }
        } stage("deploy"){
            steps{
                echo "hello deploy"
            }
        }
    }
}